# wecsimulator
A wave energy simulator where you can change the specifications and implement to your own project. 
To run, download required libraries mentioned in the first lines of the code, then create a virtual environment in python using terminal.
Afterwards, change the directory to the downloads folder by typing "cd downloads"
Finally, run the application by using the command "streamlit run app3.py"
